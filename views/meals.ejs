<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <title>Meal and Prices</title>
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="mb-4">Random Meal</h1>
        <div class="card mb-4">
            <div class="card-body">
                <h2 class="card-title text-primary"><%= mealData.strMeal %></h2>
                <img src="<%= mealData.strMealThumb %>" class="img-fluid rounded" alt="Meal image">

                <table class="table borderless mt-3">
                    <thead>
                        <tr>
                            <th>Ingredient</th>
                            <th>Quantity</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let i = 1; i <= 20; i++) { %>
                            <% 
                                const ingredient = mealData[`strIngredient${i}`];
                                const measure = mealData[`strMeasure${i}`];
                                if (ingredient && ingredient.trim()) { 
                            %>
                            <tr>
                                <td><%= ingredient %></td>
                                <td><%= measure %></td>
                            </tr>
                            <% } %>
                        <% } %>
                    </tbody>
                </table>

                <p class="mt-3"><%= mealData.strInstructions %></p>
            </div>
        </div>
    </div>
</body>
</html>
