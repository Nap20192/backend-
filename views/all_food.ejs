<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Meals</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/style.css">
</head>

<body>
  
  <%- include('partials/navbar-lang') %>
  <main>
    <div class="container">
      <div class="row">
        <h1 class="text-center my-4">Created Content</h1>
        <% if (food === null) { %> 
          <div class="error-msg">
            <p>Couldn't find anything.</p>
          </div>
        <% } else { %>
          <% food.forEach(f => { %>
            <div class="col-md-4 mb-4">
              <div class="card">
                <img src="<%= f.image%>" class="card-img-top">
                <div class="card-body">
                  <h5 class="card-title"><%= f.name %></h5>
                  <% if (page.lang !== 'en') { %>
                    <p class="card-text"><%= f.descriptionRU.substring(0, 100) %>...</p>
                  <% } else { %>
                    <p class="card-text"><%= f.descriptionEN.substring(0, 100) %>...</p>
                  <% } %>
                  
                  <a href="/posts/<%= page.lang %>/<%= f.id %>" class="btn">View</a>
                </div>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>